The video section is now a real HTML5 video.

File:
public/cybersecurity-loop.mp4

The React component uses:
autoPlay + muted + loop + playsInline

Muted autoplay is used so modern browsers permit automatic playback.
Replace the MP4 with your own approved cybersecurity video if desired.
